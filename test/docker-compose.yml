services:
  mqtttest:
    image: mqtttest:latest
    build:
      context: .
      dockerfile: Dockerfile
    entrypoint: ["/godocker"]
    environment:
      - TZ=Asia/Shanghai
    ports:
      - 11111:11111
    networks:
      - iot-net
networks:
  iot-net:
    driver: bridge
