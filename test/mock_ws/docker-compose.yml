services:
  mock-ws:
    image: mock_ws:latest
    build:
      context: .
      dockerfile: Dockerfile
    entrypoint: ["/godocker"]
    environment:
      - TZ=Asia/Shanghai
    ports:
      - 11114:11111
    networks:
      - iot-net
networks:
  iot-net:
    driver: bridge
