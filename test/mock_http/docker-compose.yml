services:
  mock-http:
    image: mock_http:latest
    build:
      context: .
      dockerfile: Dockerfile
    entrypoint: ["/godocker"]
    environment:
      - TZ=Asia/Shanghai
    ports:
      - 11113:11111
    networks:
      - iot-net
networks:
  iot-net:
    driver: bridge
