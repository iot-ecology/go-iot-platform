# 生命周期管理

```mermaid
classDiagram
    class Product {
        -string Name
        -string Description
        -string SKU
        -float64 Price
        -float64 Cost
        -int Quantity
        -int MinimumStock
        -int WarrantyPeriod
        -string Status
        -string Tags
        -string ImageURL
        -gorm.Model
    }
    class DeviceInfo {
        -uint ProductId
        -string SN
        -time.Time ManufacturingDate
        -time.Time ProcurementDate
        -int Source
        -time.Time WarrantyExpiry
        -int PushInterval
        -float64 ErrorRate
        -string Protocol
        -gorm.Model
    }
    class DeviceGroup {
        -string Name
        -gorm.Model
    }
    class DeviceGroupDevice {
        -uint DeviceInfoId
        -uint DeviceGroupGroupId
        -gorm.Model
    }
    class DeviceInstallRecord {
        -gorm.Model
        -uint DeviceGroupGroupId
        -uint DeviceInfoId
        -time.Time InstallDate
        -string Technician
        -string Description
        -string PhotoURL
    }
    class RepairRecord {
        -uint DeviceGroupGroupId
        -uint DeviceInfoId
        -time.Time RepairDate
        -string Technician
        -float64 Cost
        -string Description
        -gorm.Model
    }
    class ShipmentRecord {
        -gorm.Model
        -time.Time ShipmentDate
        -string Technician
        -string CustomerName
        -string CustomerPhone
        -string CustomerAddress
        -string TrackingNumber
        -string Status
        -string Description
    }
    class ShipmentProductDetail {
        -gorm.Model
        -uint ShipmentRecordId
        -uint ProductID
        -uint DeviceInfoId
        -int Quantity
    }
    class ProductionPlan {
        -gorm.Model
        -string Name
        -time.Time StartDate
        -time.Time EndDate
        -string Description
        -string Status
    }
    class ProductPlan {
        -gorm.Model
        -uint ProductionPlanID
        -uint ProductID
        -int Quantity
    }

    Product "1" -- "0..*" DeviceInfo : contains >
    DeviceInfo "1" -- "0..*" DeviceGroupDevice : contains >
    DeviceGroup "1" -- "0..*" DeviceGroupDevice : contains >
    DeviceInfo "1" -- "0..*" DeviceInstallRecord : contains >
    DeviceInfo "1" -- "0..*" RepairRecord : contains >
    ShipmentRecord "1" -- "0..*" ShipmentProductDetail : contains >
    ProductionPlan "1" -- "0..*" ProductPlan : contains >
```





## 业务逻辑

逻辑1：

1.   创建产品
2.   创建生产计划
     1.   生产计划中需要选择具体的产品和数量
3.   生产计划确认完成后可以在设备列表中看到本次生产计划生产的设备信息

逻辑2：

1.   创建设备组
2.   将设备列表中的设备自由选择到一个组中进行关系建立

逻辑3：

1.   创建发货记录
2.   选择产品
3.   选择具体设备

>   注意: 这块选择具体设备还没有程序支持

逻辑4：

1.   创建维修记录（这是一个普通的增删改查）

逻辑5：
1. SIM 卡管理（这是一个普通的增删改查）

